import { CircleCalcPipe } from './circle-calculator.pipe';

describe('CircleCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new CircleCalcPipe();
    expect(pipe).toBeTruthy();
  });
});
