Similar to Factory Based but in this we use .clone() method to create an object by copying bit by bit of an already created vehicle.

Student s1 = new Student();
Student s2 = s1.clone();
Student s3 = s2.clone();
